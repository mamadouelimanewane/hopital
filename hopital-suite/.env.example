# ════════════════════════════════════════════════════════════════
#  Socle du parcours patient — variables d'environnement
#  Copier vers .env.local (jamais versionné).
# ════════════════════════════════════════════════════════════════

# Postgres. Fonctionne avec Neon, Supabase, ou une instance locale.
DATABASE_URL=postgres://utilisateur:motdepasse@hote:5432/ndamatou

# Mettre à 1 pour un Postgres local sans TLS.
# PGSSL_DISABLE=1

# Le serveur de test local n'accepte qu'une connexion.
# PGPOOL_MAX=1
